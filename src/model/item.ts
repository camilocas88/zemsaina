export class Item {
  constructor(
    public _about: string,
    public accessURL: string,
    public byteSize: number,
    public format: string,
    public title: [],
    public type: string
  ) {}
}
